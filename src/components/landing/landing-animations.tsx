'use client';

import { useLayoutEffect } from 'react';
import { gsap } from 'gsap';
import { ScrollTrigger } from 'gsap/ScrollTrigger';

export function LandingAnimations() {
  useLayoutEffect(() => {
    // This component can be used for other page-wide animations if needed.
    // The interactive demo animation has been moved to be handled by click events.
    return () => {
        // cleanup if needed
    };
  }, []);

  return null;
}
